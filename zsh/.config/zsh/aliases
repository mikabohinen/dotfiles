alias ll='ls -lah'

# Git
alias ga='git add'
alias gc='git commit'
alias gp='git push'

# Neovim
alias v='nvim'
alias vim='nvim'

# Directory stack
alias d='dirs -v'
for index ({1..9}) alias "$index"="cd +${index}"; unset index
